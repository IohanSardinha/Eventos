<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.sardinha.iohan.eventos">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NovoEventoActivity"
            android:label="@string/novo_evento" />
        <activity android:name=".DetalhesEventoActivity"
            android:label="Detalhes"/>
        <activity android:name=".EventosActivity"
            android:label="Eventos"/>
        <activity android:name=".NovoUsuarioAcivity"
            android:label="Novo usuário"/>
        <activity android:name=".UsuarioActivity"
            android:label="Detalhes"></activity>
    </application>

</manifest>